import React, { Component } from 'react';

// Styles.

import "./Style.css";

// Component

class Type extends Component {
    constructor(props) {
        super(props);
        this.state = {
        };
    }

    render() {
        return (
            <div>
                Type
            </div>
        );
    }
}

export default Type;
